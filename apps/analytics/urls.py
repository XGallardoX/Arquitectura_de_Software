from django.urls import path
from . import views

app_name = 'analytics'

urlpatterns = [
   # Panel principal de reportes (placeholder por ahora)
   # path('', views.reportes_panel, name='reportes_panel'),
]
